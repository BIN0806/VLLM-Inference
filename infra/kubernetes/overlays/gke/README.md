# Overlay: GKE (example, unvalidated)

GPU node labels/tolerations, storage, load balancer/gateway, and workload identity placeholders.
Never claim this overlay is validated until it runs on real GKE GPU nodes.
