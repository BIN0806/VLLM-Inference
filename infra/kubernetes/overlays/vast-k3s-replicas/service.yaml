apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: inference
  labels: &id001
    app: inference-platform
    component: vllm
spec:
  type: ClusterIP
  selector: *id001
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
